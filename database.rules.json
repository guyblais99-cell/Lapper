{
  "rules": {
    "recitals": {
      "$recitalId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    },
    "users": {
      "$uid": {
        ".read": "auth != null && auth.uid == $uid",
        ".write": "auth != null && auth.uid == $uid"
      }
    },
    "apextrack": {
      "$sessionId": {
        "meta": {
          ".read": "auth != null",
          ".write": "auth != null"
        },
        "triggers": {
          ".read": "auth != null",
          ".write": "auth != null",
          "$triggerId": {
            ".read": "auth != null",
            ".write": "auth != null"
          }
        }
      }
    }
  }
}
